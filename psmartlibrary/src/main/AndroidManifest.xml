<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kenyahmis.psmartlibrary">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.DeviceScanActivity"
            android:label="DeviceScanActivity"></activity>
        <activity
            android:name=".Activities.ReaderSettingsActivity"
            android:label="ReaderSettingsActivity"></activity>
    </application>

</manifest>